# Banco de dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dados_rfb
DB_USER=rfb_user
DB_PASSWORD=

# API
PORT=3001

# Admin — chave para endpoints administrativos
ADMIN_KEY=troque-esta-chave-em-producao

# ETL — diretórios de trabalho
ETL_DOWNLOAD_DIR=./etl-data/downloads
ETL_EXTRACT_DIR=./etl-data/extraidos

# CRM — para integração reversa (opcional)
CRM_API_URL=http://localhost:3000
CRM_API_TOKEN=
